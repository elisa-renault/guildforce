# Memory: technical/code-quality-v3

Code Quality & Maintenance: 1) Internationalization (i18n): **AUDIT 100% COMPLETE** — All UI strings centralized in 'src/i18n/translations.ts'. Full coverage for Admin, Forum, Polls, Permissions, Profile, Wishes, Roster, Dashboard, and Accessibility sections. All 'isFrench' ternaries and 'language === fr' patterns replaced with 't.*' keys. Only exceptions: 'dateLocale' patterns (date-fns configuration) and 'title_fr/en' database fields (content, not UI). 2) OAuth: Shared logic for Battle.net authentication centralized in 'src/lib/battlenetOAuth.ts' to handle state parsing, validation, and cleanup. 3) Utilities: 'src/lib/rankUtils.ts' factorizes rank-related logic (index generation, sorting, name mapping) used across permission and profile sliders. 4) Logging: Mandatory use of 'src/lib/logger.ts' instead of 'console.error' for production-safe error tracking across ~35 core files. 5) TypeScript: Permissive configuration (noImplicitAny: false) remains due to read-only 'tsconfig.json' constraints.
